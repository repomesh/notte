---
title: Quickstart
description: 'Get started in a few seconds'
mode: "wide"
---

import InstallSDK from '/snippets/quickstart/install_sdk.mdx';
import SetApiKey from '/snippets/quickstart/set_api_key.mdx';

export const notteSetupPrompt = [
  "# Setup Notte",
  "",
  "## Prerequisites",
  "- Review the Notte skill documentation for available commands and capabilities:  ",
  "  https://raw.githubusercontent.com/nottelabs/notte-skills/refs/heads/main/plugins/notte-cli/skills/notte-browser/SKILL.md",
  "",
  "- Install the skill globally:",
  "",
  "```bash",
  "npx skills add nottelabs/notte-skills -y -g",
  "```",
  "",
  "---",
  "",
  "## Installation & Authentication Steps",
  "",
  "### 1. Verify the Notte CLI Installation",
  "",
  "Run:",
  "",
  "```bash",
  "notte -h",
  "```",
  "",
  "If the command is not found, install the CLI with:",
  "",
  "```bash",
  "brew tap nottelabs/notte-cli https://github.com/nottelabs/notte-cli.git",
  "brew install notte",
  "```",
  "",
  "---",
  "",
  "### 2. Check Authentication Status",
  "",
  "Run:",
  "",
  "```bash",
  "notte auth status",
  "```",
  "",
  "- If you are already authenticated, continue to the next step.",
  "- If authentication is required, run:",
  "",
  "```bash",
  "notte auth login",
  "```",
  "",
  "Complete the login flow in your browser.",
  "",
  "Then poll the authentication status every 5 seconds for up to 5 minutes:",
  "",
  "```bash",
  "notte auth status",
  "```",
  "",
  "If authentication does not complete after 5 minutes, stop and ask the user for assistance.",
  "",
  "---",
  "",
  "### 3. Start a Browser Session",
  "",
  "Launch a browser session, open the viewer, and navigate to the documentation site:",
  "",
  "```bash",
  "notte sessions start && notte sessions viewer && notte page goto https://docs.notte.cc/",
  "```",
  "",
  "Echo the session viewer url to the user in case the page doesn't automatically open on their desktop.",
].join("\n");

<Visibility for="humans">

## Fast setup

The recommended path is to start with the Notte skill and CLI inside your AI coding agent. It helps discover browser flows, test selectors, inspect page results, and pivot to SDK code when the workflow is ready.

<div className="notte-fast-setup">
  <div className="notte-fast-setup__body">
    <p>
      Copy this into your AI coding agent (Codex, Claude Code, Cursor, etc.). It installs the Notte skill, checks the CLI, authenticates your account, and opens a live browser session that you or your agent can control.
    </p>
    <button
      type="button"
      className="notte-fast-setup__button"
      onClick={(event) => {
        const button = event.currentTarget;
        const label = button.querySelector(".notte-fast-setup__button-label");

        navigator.clipboard.writeText(notteSetupPrompt);
        button.classList.add("is-copied");

        if (label) {
          label.textContent = "Copied";
        }

        window.setTimeout(() => {
          button.classList.remove("is-copied");

          if (label) {
            label.textContent = "Copy setup prompt";
          }
        }, 1600);
      }}
    >
      <span className="notte-fast-setup__button-icon" aria-hidden="true" />
      <span className="notte-fast-setup__button-label" aria-live="polite">Copy setup prompt</span>
    </button>
  </div>
</div>

<AccordionGroup>
  <Accordion title="View setup prompt">
    ````markdown
    # Setup Notte

    ## Prerequisites
    - Review the Notte skill documentation for available commands and capabilities:
      https://raw.githubusercontent.com/nottelabs/notte-skills/refs/heads/main/plugins/notte-cli/skills/notte-browser/SKILL.md

    - Install the skill globally:

    ```bash
    npx skills add nottelabs/notte-skills -y -g
    ```

    ---

    ## Installation & Authentication Steps

    ### 1. Verify the Notte CLI Installation

    Run:

    ```bash
    notte -h
    ```

    If the command is not found, install the CLI with:

    ```bash
    brew tap nottelabs/notte-cli https://github.com/nottelabs/notte-cli.git
    brew install notte
    ```

    ---

    ### 2. Check Authentication Status

    Run:

    ```bash
    notte auth status
    ```

    - If you are already authenticated, continue to the next step.
    - If authentication is required, run:

    ```bash
    notte auth login
    ```

    Complete the login flow in your browser.

    Then poll the authentication status every 5 seconds for up to 5 minutes:

    ```bash
    notte auth status
    ```

    If authentication does not complete after 5 minutes, stop and ask the user for assistance.

    ---

    ### 3. Start a Browser Session

    Launch a browser session, open the viewer, and navigate to the documentation site:

    ```bash
    notte sessions start && notte sessions viewer && notte page goto https://docs.notte.cc/
    ```

    Echo the session viewer url to the user in case the page doesn't automatically open on their desktop.
    ````
  </Accordion>
</AccordionGroup>

---

## SDK quickstart

Prefer the old-school route? This setup is for humans who want to write code directly, avoid handing the keyboard to an LLM, and keep every import, API key, and browser session under manual control.

<Steps>
  <Step title="Get an API key">
    Create an account on the [Console](https://console.notte.cc) to get your API key.
  </Step>
  <Step title="Export your API key">
    <SetApiKey />
  </Step>
  <Step title="Install the SDK">
    <InstallSDK />
  </Step>
</Steps>

Now that you're set up, here are the main SDK topics to start with.

<CardGroup cols={3}>
  <Card title="Browser Session" icon="tv" href="/concepts/sessions#quick-start">
    Connect to a cloud browser via CDP
  </Card>
  <Card title="Web Agent" icon="microchip-ai" href="/concepts/agents#quick-start">
    Run an AI agent on the browser
  </Card>
  <Card title="Scraping" icon="database" href="/concepts/scraping#quick-start">
    Extract structured data from a page
  </Card>
</CardGroup>

</Visibility>

<Visibility for="agents">

# Setup Notte

## Prerequisites
- Review the Notte skill documentation for available commands and capabilities:
  https://raw.githubusercontent.com/nottelabs/notte-skills/refs/heads/main/plugins/notte-cli/skills/notte-browser/SKILL.md

- Install the skill globally:

```bash
npx skills add nottelabs/notte-skills -y -g
```

---

## Installation & Authentication Steps

### 1. Verify the Notte CLI Installation

Run:

```bash
notte -h
```

If the command is not found, install the CLI with:

```bash
brew tap nottelabs/notte-cli https://github.com/nottelabs/notte-cli.git
brew install notte
```

---

### 2. Check Authentication Status

Run:

```bash
notte auth status
```

- If you are already authenticated, continue to the next step.
- If authentication is required, run:

```bash
notte auth login
```

Complete the login flow in your browser.

Then poll the authentication status every 5 seconds for up to 5 minutes:

```bash
notte auth status
```

If authentication does not complete after 5 minutes, stop and ask the user for assistance.

---

### 3. Start a Browser Session

Launch a browser session, open the viewer, and navigate to the documentation site:

```bash
notte sessions start && notte sessions viewer && notte page goto https://docs.notte.cc/
```

Echo the session viewer url to the user in case the page doesn't automatically open on their desktop.

</Visibility>
