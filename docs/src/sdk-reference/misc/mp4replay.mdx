---
title: "MP4Replay"
description: "Local MP4 replay data wrapper (for local browser sessions)"
---

<Note>
  This class is used internally for local session replays. For cloud/API replays,
  see <Visibility for="humans">[`ReplayResponse`](/sdk-reference/misc/replayresponse)</Visibility><Visibility for="agents">[`ReplayResponse`](/sdk-reference/misc/replayresponse.md)</Visibility> which provides presigned
  URLs for downloading MP4 files.
</Note>



## Methods

### save

```python
save(output_file: <class 'str'>)
```

Save the mp4 replay to a file

**Parameters:**

- `output_file`: Path where to save the mp4 file. Must end with .mp4 extension.

**Raises:**

- `ValueError`: If the output file doesn't have a .mp4 extension.

---



## Module

`notte_core.utils.webp_replay`
